# 寫作風格守則

這份教材由多人／多次增修，需要一致的語氣。以下規則來自「原作者章節」與「後續增補章節」的實際比對——後者有明顯的 AI 寫作習氣，這份守則就是用來壓掉它。

## 量化目標（每千字）

| 項目 | 原作者章節 | 增補章節（改前） | 目標 |
|---|---|---|---|
| 破折號 `——` | 0.3 | 1.9 | **≤ 0.6** |
| 引言塊 `>` | 0.0 | 1.6 | **≤ 0.4**，且只用於下列兩種 |
| 粗體 `**…**` | 6.7 | 9.2 | **≤ 7** |
| 口頭禪 | 0.1 | 0.7 | **≤ 0.1** |

改後實測（增補章節）：破折號 0.1、引言塊 0.2、粗體 6.0、口頭禪 0.0。

## 規則

### 1. 引言塊只有兩種正當用途

- **安全或技術陷阱的警告**：`⚠️ 回授量務必小於 1，否則音量會無限累積`
- **真正的引文**：別人說過的話

**不可以**把自己的一句話抽出來加粗放進引言塊當「金句」。那是簡報的手法，不是教材的手法。想強調就把它寫成一個好句子，放在段落裡。

### 2. 破折號不是萬用連接號

`——` 只用在真正的補述或轉折。以下情況一律改寫：

- 用來接一句總結（「…，這正是限制長出風格的例子——」）→ 直接寫成句子
- 用來製造停頓感 → 用句號分成兩句
- 一段裡出現第二個 `——` → 一定要改掉一個

### 3. 粗體標記名詞，不標記情緒

- 可以：第一次出現的術語、表格裡的關鍵欄位、物件名
- 不可以：整句加粗、結論句加粗、「**這才是重點**」這類語氣加粗

### 4. 不要每一節都有結尾金句

段落結束就結束。只有在該節真的有一個讀者容易忽略的判斷時，才寫一句提醒，而且用平常的句子寫。

### 5. 避免的口頭禪

值得注意的是／其實／正是／換句話說／真正的 X 是／不是 X，而是 Y／誠實地說／某種意義上／本質上

這些不是禁用詞，是**濫用**的警訊。一章出現一兩次沒問題，每節都有就是習氣。

### 6. 交叉引用要克制

一段裡最多一個「見 X.Y 節」。讀者不需要每個名詞都被連回去。

### 7. 用詞

- 台灣用語：程式（非程序）、軟體（非軟件）、資料（非數據，但「數據處理」是 Max 的既有譯名，沿用）、訊號（非信號）
- 原作者章節部分沿用中國大陸譯詞（編程、軟件、信息），修訂時可順手改，但不必為此重寫整段
- 專有名詞第一次出現給原文：包絡（envelope）、取樣（sampling）

### 8. 投影片不適用第 1、2、4 條

簡報本來就靠短句與強調撐版面。`slides/` 底下的檔案可以用引言塊與粗體，但仍要避免第 5 條的口頭禪。

### 9. 不要寫成翻譯腔

以下句式讀起來像英文直譯，一律改寫：

| 別寫 | 改成 |
|---|---|
| 當你在 Pd 裡拉一條線，你就在做這個決定。 | 你在 Pd 裡拉的每一條線，都是在做這個決定。 |
| 以下都是現在就已經在發生的事，不是預測。 | 下面談的都已經在發生，並不是對未來的猜測。 |
| 你示範幾次「這個手勢對應這個聲音」，它自己學會映射。 | 你只要示範幾次某個手勢該對應到什麼聲音，它就能自己歸納出兩者的對應關係。 |

要避免的具體句型：

- **「當你 X，你就 Y」**——英文 When/If you… 的直譯。改成主詞在前的一般敘述。
- **「不是 X，而是 Y」**——偶爾用可以，但不能每節都出現。多數時候直接說 Y 就好。
- **「……，不是……。」當段落結尾**——這是為了製造反差的修辭，不是中文的自然說法。
- **「真正的 X 是……」「X 才是……」**——同上。
- **「這正是……的原點／來源／關鍵」**——刪掉「正是」，或整句改寫。

### 10. 不要刻意斷短句

為了製造節奏而把一句話切成三句、或讓一個五個字的句子獨立成段，是簡報的手法。教材要寫成能順順讀下去的段落。

- 段落裡不要出現獨立的超短句（少於十四個字）當作結論
- 該用完整的長句就用，中文可以承受比英文更長的句子
- 判準：把段落唸出來，如果聽起來像在下標題或喊口號，就是斷太短了

## 檢查方式

```bash
python3 - <<'PY'
import re, pathlib
TICS=["值得","其實","正是","誠實地說","換句話說","關鍵在","重點是","真正的","本質上","某種意義上"]
for p in sorted(pathlib.Path('chapters').glob('*.md')):
    t=p.read_text(encoding='utf-8'); n=len(t)/1000
    calque = len(re.findall(r'當你[^。\n]{2,40}?，你(就|也|已經)',t)) + len(re.findall(r'不是[^。\n]{2,30}?，(而)?是',t))
    print(f"{p.name:<30} 破折號{t.count('——')/n:5.1f}  引言{len(re.findall(chr(94)+'>',t,re.M))/n:5.1f}"
          f"  粗體{len(re.findall(r'[*][*][^*]+[*][*]',t))/n:5.1f}  口頭禪{sum(t.count(x) for x in TICS)/n:5.1f}  翻譯腔{calque:3d}")
PY
```
